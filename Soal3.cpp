#include<iostream>
using namespace std;

int main(){
    int angka[] = {5, 1, 4, 2, 8};
    int[i] = {"[a]", "[b]", "[c]","[d]","[e]"};
    int dtsort, hasilsort;
    for (int[i] == 1, i < [a], i++ );
    while (int[i] == 0, i > [b], i++);
    cout << "\nHasil sort" << endl;
}
